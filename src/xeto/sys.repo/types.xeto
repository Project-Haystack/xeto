//
// Copyright (c) 2026, Project-Haystack
// Licensed under the Academic Free License version 3.0
//
// History:
//   3 Aug 2026  Brian Frank  Creation
//

// Response from [repoPing()] to verify end-to-end connectivity
// with the repo.  Servers may add their own tags.
RepoPing: Dict {

  // Repo display name
  dis: Str
}

// Summary of one lib version available in a repo as reported by
// [repoSearch()] and [repoVersions()].
RepoLib: Dict {

  // Dotted lib name
  lib: Str

  // Lib version
  version: Version

  // Publication lifecycle status
  pubStatus: RepoPubStatus

  // Additional message associated with publication lifecycle status
  pubStatusMsg: Str?

  // Summary documentation from the lib meta
  doc: Str?

  // Lib dependencies; omitted when not known such as in search results
  depends: List? <of:LibDepend>

  // Digest of the lib's xetolib zip file formatted as "sha256:"
  // followed by the base64uri encoding of the SHA-256 hash.  Clients
  // should verify fetched content against this digest when available.
  digest: Str?
}

// Publication lifecycle status of a lib version as reported by the
// repo.  This is mutable repo catalog state, not metadata of the lib
// artifact itself.
RepoPubStatus: Enum {
  alpha     // Early experimental prerelease
  beta      // Feature complete prerelease
  stable    // Stable production release - used to determine latest and matching
  yanked    // Withdrawn from new resolution
}

// Search results with paging metadata as reported by [repoSearch()].
RepoSearch: Dict {

  // Matching libs for this page reporting the latest version of each lib
  libs: List <of:RepoLib>

  // Total count of matching libs across all pages
  total: Int

  // Actual limit applied by the server
  limit: Int

  // Offset of this page into the total matches
  offset: Int
}

