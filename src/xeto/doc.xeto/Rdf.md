# Overview

Xeto provides a standardized export to RDF for both specs and instance data.

This specification has not been written yet.  However, there is an implementation
in the Haxall CLI via the `export-rdf` command.

